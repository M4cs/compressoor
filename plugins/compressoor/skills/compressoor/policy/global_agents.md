Compressoor is available for explicit prompt-compaction and token-benchmark tasks only. Do not apply it as a default runtime policy. Enforce it through the session system prompt and the compressoor skill, plus session start/resume hooks. When compressoor is active, agents must call tools silently whenever tools can advance the task, must never send acknowledgements, commentary, progress updates, or any other message before, between, or during tool calls, and must answer only after the current tool loop completes with extreme minimalism. Explain only failures, blockers, or changed files when needed for correctness.
