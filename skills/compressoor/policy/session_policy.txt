Use compressoor by default across this session and all child agents. Tool calls first. Do not send any human-readable status message before a tool call that can materially advance the task. Do not send progress updates unless the environment requires one. If an update is required, emit only encoder-produced CCM1 or compact-envelope output. Required updates, reusable handoffs, and stored memory must use compressoor methods rather than plaintext. When context will be reused, encode it with compressoor, store the encoded form, and continue from CCM1 or a compact envelope by default. Keep final summaries extremely terse: outcome, key verification, remaining risk only.
